// operators
// == === != !==
const one = 1;
const oneStr = '1';
const result = one === oneStr;

// console.log(result);
const two = 2;
console.log('result of one == oneStr ', one == oneStr);

// ternary operator
const cityName = 'Lviv';

const welcomeMsg = cityName === 'Kyiv' ? 'Welcome to Kyiv!' : 'Welcome to Ukraine!';

console.log('ternary result - ', welcomeMsg);